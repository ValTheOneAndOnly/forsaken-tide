name: Keep Alive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Forsaken Tide
        run: curl -s -o /dev/null -w "%{http_code}" "https://forsaken-tide.onrender.com/"
